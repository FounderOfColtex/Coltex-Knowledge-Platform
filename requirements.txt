# Core ML stack (HuggingFace fine-tuning path)
torch>=2.1.0
transformers>=4.44.0
datasets>=2.20.0
trl>=0.9.6
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
tokenizers>=0.19.0

# Data preparation & corpus generation
pyyaml>=6.0.1
tqdm>=4.66.0

# Optional: experiment tracking
# wandb>=0.17.0
