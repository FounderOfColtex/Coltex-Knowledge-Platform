# Postgresql Domain

Part of the **Coltex Living Brain** — `postgresql` knowledge cluster.

Documents here are auto-generated, graph-linked, and indexed by the Coltex brain.
Each file carries typed metadata (`doc_type`, `hub`, `related`) for GraphRAG traversal.

## Stats
- Category: `postgresql`
- Parent: `living-brain/domains/postgresql/`

Query this domain:
```bash
python3 -m brain retrieve "your question about postgresql"
```
