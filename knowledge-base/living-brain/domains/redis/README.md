# Redis Domain

Part of the **Coltex Living Brain** — `redis` knowledge cluster.

Documents here are auto-generated, graph-linked, and indexed by the Coltex brain.
Each file carries typed metadata (`doc_type`, `hub`, `related`) for GraphRAG traversal.

## Stats
- Category: `redis`
- Parent: `living-brain/domains/redis/`

Query this domain:
```bash
python3 -m brain retrieve "your question about redis"
```
