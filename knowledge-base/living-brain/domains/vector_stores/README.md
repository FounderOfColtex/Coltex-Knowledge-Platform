# Vector Stores Domain

Part of the **Coltex Living Brain** — `vector_stores` knowledge cluster.

Documents here are auto-generated, graph-linked, and indexed by the Coltex brain.
Each file carries typed metadata (`doc_type`, `hub`, `related`) for GraphRAG traversal.

## Stats
- Category: `vector_stores`
- Parent: `living-brain/domains/vector_stores/`

Query this domain:
```bash
python3 -m brain retrieve "your question about vector_stores"
```
