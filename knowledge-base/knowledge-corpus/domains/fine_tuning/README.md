# Fine Tuning Domain

Part of the **Coltex Knowledge Corpus** — `fine_tuning` knowledge cluster.

Documents here are auto-generated, graph-linked, and indexed by the Coltex brain.
Each file carries typed metadata (`doc_type`, `hub`, `related`) for GraphRAG traversal.

## Stats
- Category: `fine_tuning`
- Parent: `knowledge-corpus/domains/fine_tuning/`

Query this domain:
```bash
python3 -m brain retrieve "your question about fine_tuning"
```
