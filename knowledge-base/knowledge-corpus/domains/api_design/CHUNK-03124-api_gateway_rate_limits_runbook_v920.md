---
id: CHUNK-03124-API-GATEWAY-RATE-LIMITS-RUNBOOK-V920
title: "Chunk 03124 API Gateway: Rate Limits \u2014 Runbook (v920)"
category: CHUNK-03124-api_gateway_rate_limits_runbook_v920.md
tags:
- api_gateway
- rate limits
- api_design
- runbook
- api_design
- variant_920
difficulty: intermediate
related:
- CHUNK-03123
- CHUNK-03122
- CHUNK-03121
last_updated: '2026-07-07'
version: '2.0'
---

---
id: CHUNK-03124
title: "API Gateway: Rate Limits \u2014 Runbook (v920)"
category: api_design
doc_type: runbook
tags:
- api_gateway
- rate limits
- api_design
- runbook
- api_design
- variant_920
difficulty: intermediate
related: []
last_updated: '2026-07-07'
version: '2.0'
hub: api_gateway
---

# API Gateway: Rate Limits — Runbook (v920)

## Trigger

In practice, **Trigger** for `API Gateway: Rate Limits` (runbook). This variant 920 covers api_gateway, rate limits, api_design at intermediate level. Key considerations include reliability, observability, latency budgets, and safe rollout. Teams should validate assumptions with benchmarks, add tracing spans, and document failure modes. Recommended metrics: p95 latency, error rate, recall@k (if retrieval), and freshness of indexed data.
## Severity

In practice, **Severity** for `API Gateway: Rate Limits` (runbook). This variant 920 covers api_gateway, rate limits, api_design at intermediate level. Key considerations include reliability, observability, latency budgets, and safe rollout. Teams should validate assumptions with benchmarks, add tracing spans, and document failure modes. Recommended metrics: p95 latency, error rate, recall@k (if retrieval), and freshness of indexed data.
## Immediate Actions

In practice, **Immediate Actions** for `API Gateway: Rate Limits` (runbook). This variant 920 covers api_gateway, rate limits, api_design at intermediate level. Key considerations include reliability, observability, latency budgets, and safe rollout. Teams should validate assumptions with benchmarks, add tracing spans, and document failure modes. Recommended metrics: p95 latency, error rate, recall@k (if retrieval), and freshness of indexed data.
## Escalation

In practice, **Escalation** for `API Gateway: Rate Limits` (runbook). This variant 920 covers api_gateway, rate limits, api_design at intermediate level. Key considerations include reliability, observability, latency budgets, and safe rollout. Teams should validate assumptions with benchmarks, add tracing spans, and document failure modes. Recommended metrics: p95 latency, error rate, recall@k (if retrieval), and freshness of indexed data.
## Recovery Verification

In practice, **Recovery Verification** for `API Gateway: Rate Limits` (runbook). This variant 920 covers api_gateway, rate limits, api_design at intermediate level. Key considerations include reliability, observability, latency budgets, and safe rollout. Teams should validate assumptions with benchmarks, add tracing spans, and document failure modes. Recommended metrics: p95 latency, error rate, recall@k (if retrieval), and freshness of indexed data.
