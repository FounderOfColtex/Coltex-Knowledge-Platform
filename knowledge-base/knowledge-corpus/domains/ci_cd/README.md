# Ci Cd Domain

Part of the **Coltex Knowledge Corpus** — `ci_cd` knowledge cluster.

Documents here are auto-generated, graph-linked, and indexed by the Coltex brain.
Each file carries typed metadata (`doc_type`, `hub`, `related`) for GraphRAG traversal.

## Stats
- Category: `ci_cd`
- Parent: `knowledge-corpus/domains/ci_cd/`

Query this domain:
```bash
python3 -m brain retrieve "your question about ci_cd"
```
